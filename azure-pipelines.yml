jobs:
- job: Foo

  steps:
  - script: echo Hello!
    condition: false # this step will always run, even if the pipeline is cancelled

- job: Bar
  dependsOn: Foo
  condition: true # this job will only run if Foo fails